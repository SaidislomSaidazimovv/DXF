/**
 * R3F platform shim — native variant.
 * Metro picks this up for iOS/Android builds.
 */
export { Canvas, useFrame, useThree } from '@react-three/fiber/native';
export type { ThreeEvent } from '@react-three/fiber/native';
